import { StyleSheet } from "react-native";

export default StyleSheet.create({
    input: {
        flex: 1,
        backgroundColor: '#fff',
        borderRadius: 8,
        paddingHorizontal: 8,
    },
});